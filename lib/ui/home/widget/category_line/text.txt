SingleChildScrollView(
      scrollDirection = Axis.horizontal,
      child = Padding(
        padding: const EdgeInsets.only(left: 20.0),
        child: Row(
          mainAxisAlignment: MainAxisAlignment.spaceEvenly,
          children: [
            TextButton(
              onPressed: () {
                setState(() {
                  isSelect = !isSelect;
                });
              },
              child: Text(AppStrings.allCategory,
                  style: TextStyle(
                      color: isSelect
                          ? AppColor.priceColor
                          : AppColor.standartCatergoryColor)),
            ),
            TextButton(
              onPressed: () {
                setState(() {
                  isSelect = !isSelect;
                });
              },
              child: Text(
                AppStrings.hoodieCategory,
                style: TextStyle(
                    color: isSelect
                        ? AppColor.priceColor
                        : AppColor.standartCatergoryColor),
              ),
            ),
            TextButton(
              onPressed: () {
                setState(() {
                  isSelect = !isSelect;
                });
              },
              child: Text(
                AppStrings.jacketCategory,
                style: TextStyle(
                    color: isSelect
                        ? AppColor.priceColor
                        : AppColor.standartCatergoryColor),
              ),
            ),
            TextButton(
              onPressed: () {
                setState(() {
                  isSelect = !isSelect;
                });
              },
              child: Text(
                AppStrings.pantsCategory,
                style: TextStyle(
                    color: isSelect
                        ? AppColor.priceColor
                        : AppColor.standartCatergoryColor),
              ),
            ),
            TextButton(
              onPressed: () {
                setState(() {
                  isSelect = !isSelect;
                });
              },
              child: Text(
                AppStrings.tshirtCategory,
                style: TextStyle(
                    color: isSelect
                        ? AppColor.priceColor
                        : AppColor.standartCatergoryColor),
              ),
            ),
            TextButton(
              onPressed: () {
                setState(() {
                  isSelect = !isSelect;
                });
              },
              child: Text(
                AppStrings.shirtCategory,
                style: TextStyle(
                    color: isSelect
                        ? AppColor.priceColor
                        : AppColor.standartCatergoryColor),
              ),
            ),
            TextButton(
              onPressed: () {
                setState(() {
                  isSelect = !isSelect;
                });
              },
              child: Text(
                AppStrings.outwearCategory,
                style: TextStyle(
                    color: isSelect
                        ? AppColor.priceColor
                        : AppColor.standartCatergoryColor),
              ),
            ),
            TextButton(
              onPressed: () {
                setState(() {
                  isSelect = !isSelect;
                });
              },
              child: Text(
                AppStrings.accessoriesCategory,
                style: TextStyle(
                    color: isSelect
                        ? AppColor.priceColor
                        : AppColor.standartCatergoryColor),
              ),
            ),
          ],
        ),
      ),
    );